# Local-Only Notes

This directory is for notes that should stay on your machine and out of git.

Typical use:
- production hostnames and operator shortcuts
- incident notes with sensitive detail
- one-off recovery commands
- credentials *references* (not secrets themselves)

Anything else you add here will be ignored by git.
