[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Disable strict coverage for property tests
addopts = 
    -v
    --tb=short
    --strict-markers

# Coverage settings (optional, less strict)
# --cov=cc_core
# --cov-report=html
# --cov-report=term-missing
# --cov-fail-under=80