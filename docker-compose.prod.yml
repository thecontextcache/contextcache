services:
  api:
    environment:
      - APP_ENV=prod
      - RUN_MIGRATIONS=1
      - RESEND_FROM_EMAIL=noreply@thecontextcache.com

  web:
    environment:
      - APP_ENV=prod

  # docs is dev-only — not needed in production
  # docs:
  #   profiles: ["never"]

  db:
    # Never expose Postgres publicly in production compose.
    ports: []
