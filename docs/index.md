# ContextCache (MVP)

ContextCache is a shared, opt-in project memory layer for teams using different AI tools.
Humans publish high-signal **memory cards**; the system generates **memory packs** for recall.
