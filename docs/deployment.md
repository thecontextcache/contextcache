# Deployment (Dev/Staging)

- Run API + Postgres via Docker Compose on the Ubuntu server.
- Access privately via Tailscale.
- Secrets stored in 1Password and materialized into a local .env on the server.
