# Architecture (MVP)

- **Memory Cards** are stored in Postgres (project-scoped).
- **Recall** returns a formatted "memory pack" (top relevant cards).
- Nothing is auto-saved; users intentionally publish cards.
