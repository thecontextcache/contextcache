// TODO: Implement root layout
// - Global navigation
// - Theme provider
// - Auth context
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
